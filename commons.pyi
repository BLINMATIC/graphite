def rgb2hex(rgb: list[int, int, int]) -> str:
    """Converts a rgb tuple into a hexadecimal string."""

def scalar4(a, b, c, d) -> tuple:
    """Scalar object made of 4 variables."""

def scalar3(a, b, c) -> tuple:
    """Scalar object made of 3 variables."""

def scalar2(a, b) -> tuple:
    """Scalar object made of 2 variables."""